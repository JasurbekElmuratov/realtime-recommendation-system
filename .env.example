# Leave these empty to use the local embedding cache.
# To use Cloud, fill both values and run uvicorn with --env-file .env.
QDRANT_URL=
QDRANT_API_KEY=
BOOKFEED_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
